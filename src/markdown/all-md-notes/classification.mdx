Output is categorical

The output is within a finite set, denote this set $M$

For $m \in M$ we say $m$ is a class or label

Denote some $m_i \in M$ as $1, 2, 3 ..., M$ but note that there *is no* ordering

$M = 2$ is called a binary classification and we use $-1$ and $1$ classes. labels

[[Agile]]
```mermaid
graph TD
    A[Start] --> B{Is it working?}
    B -- Yes --> C[Great!]
    B -- No --> D[Fix it]
    D --> B
```

```python
def fibonacci(n):
    a, b = 0, 1
    for _ in range(n):
        yield a
        a, b = b, a + b
```

```mermaid
graph TD
    A --> B
    B --> C
    C --> D
    D --> B
```

```mermaid
flowchart LR
    A((A)) --> B((B))
    B --> C((  Node B  ))
    C --> D((  Node A  ))
    D --> B  

    classDef highlight fill:#ff6b81,stroke:#ff4d6d,stroke-width:2px,color:#fff;
    class D highlight;
 
```






